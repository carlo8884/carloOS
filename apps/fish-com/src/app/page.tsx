/**
 * Fish.com Homepage — / (Aquarist's Tank Control Center)
 * SEE ARTIFACTS - content too large for single tool call; restoring via sequential approach
 */
export default function HomePage() { return null }
