---
from: carlo
to: carlo
status: pending
created: 2026-05-28
blockers:
next_action: "Carlo works the triage table top-down on Saturday post-launch; merge in the recommended sequence."
---

## Context

Dog.com soft-launches Friday 2026-05-29. We have 11 open PRs across content
remediation, visual polish, a new tool route, and two CI-gate proposals. Several
new branches will land overnight from parallel agents (Horses+PetFood scaffold,
fish-com metadata trim, fish-com sitemap priority fix, lizard trust-guard fix,
lizard+saddle content sprints) — none of those are in scope here, but the
Saturday plan needs to leave room for them.

The launch is Friday; **no merges happen Friday**. Saturday is the first day
this backlog can be drained.

## Inputs

- Repo: `carlo8884/carloos`, base branch `main` at `dd39dbc`
- 11 open PRs enumerated from GitHub MCP at audit time (2026-05-28)
- CI surface per PR: 6 checks for content PRs (Vercel, link-integrity,
  metadata-policy, trust-guard, verify, audit) + sitemap-fresh on #30
- Existing remediation pattern from already-merged PRs #9 (Dog), #10 (Saddle),
  #25 (Fish), #18 (postcss rollout)

## Cross-cutting CI note

`metadata-policy` is currently failing on **every PR with full CI** (#19, #22-equivalent
content PRs, #26, #27, #28, #29, #30). Cause: fish-com has 3 inherited metadata
violations on `main` that **PR #19 fixes**. Merging #19 first unblocks the
metadata-policy red across the board. This is the single most important Saturday
move.

`trust-guard` failing on #29 is **by design** — #29 introduces the patterns and
its own CI surfaces the launch-blockers that #19 + #26 clear. #29 only goes green
once #19 and #26 are in main.

`sitemap-fresh` failing on #30 is **by design** — #30 introduces the gate and
surfaces fish-com sitemap drift. Per #30 author, a separate follow-up PR is
required before #30 can merge. Out of scope for Saturday.

## Triage table

Sorted by recommended Saturday merge order. Trivial 1-file content fixes batch
first because they cascade-unblock the metadata-policy gate for everything else.

| # | Title (short) | Branch | Class | Blocker | Order | Rationale |
|---|---|---|---|---|---|---|
| **19** | fix(fish-com): clear 2 launch blockers — homepage trust stats + heater hands-on claims | `agent2/pr3d-fish-launch-blockers` | NEEDS FIX | metadata-policy red, but **PR is the fix** | **1** | Author marks "Ready to merge". Same class as already-merged #9/#10. Merging clears the inherited fish-com metadata-policy violation that's reddening every other PR. |
| **26** | PR-3d: clear Lizard.com launch blockers | `build-bot/phase3d-lizard-launch-blockers` | NEEDS FIX | inherited metadata-policy (clears after #19) | **2** | 1 file, +4/-4. Same softening-pattern as #9/#10/#19. Unblocks #29 trust-guard (along with #19). |
| **27** | PR-3e: clear Vets.co launch blockers | `build-bot/phase3e-vets-launch-blockers` | NEEDS FIX | inherited metadata-policy (clears after #19) | **3** | 1 file, +3/-3. Last of the phase-3 site-blocker sweep (Dog/Saddle/Fish already done). |
| **20** | fish.com: add /tools/equipment-recommender | `claude/fish-equipment-recommender` | READY | none (full CI not run; only Vercel) | **4** | New route, no existing-file conflicts, +459/-0 isolated under `/tools/equipment-recommender`. Sitemap append at end avoids #13-merged conflict. Re-run full CI on push; should pass cleanly. |
| **21** | A4: Vets.co visual launch-polish | `agent4/visual-pass-vets-co` | READY | none (PR #18 base already in main) | **5** | next/font wiring + hero mobile fix. Vercel green. Stacked-on parent #18 is in main, so this is now a standalone clean merge. |
| **22** | A4: Fish.com visual launch-polish | `agent4/visual-pass-fish-com` | READY | none | **6** | Same pattern as #21. next/font + mobile hero + SVG icons. Touches only fish-com app code. |
| **23** | A4: Saddle.com visual launch-polish | `agent4/visual-pass-saddle-com` | READY | none | **7** | Same pattern as #21. Bodoni + Jost wiring; biggest single-site visual upgrade. |
| **24** | A4: Lizard.com visual launch-polish | `agent4/visual-pass-lizard-com` | READY | none | **8** | Same pattern as #21. After #26 lands, rebase to pick up the lizard content cleanup before merging. |
| **28** | A4: Dog.com visual launch-polish + audit | `agent4/visual-brand-pass` | NEEDS FIX | metadata-policy red (likely clears after #19); also bundles a 247-line audit MD | **9** | Flagship site — review more carefully than #21-24. Confirm metadata-policy goes green after #19 lands; consider trimming the bundled `DESIGN-AUDIT-VISUAL.md` into a separate ops/handoff doc rather than shipping it in app PRs. |
| **29** | PR-A: trust-guard baseline expansion — hands-on testing patterns | `build-bot/trust-guard-baseline-expand` | BLOCKED-BY | #19 and #26 (both must merge first) | **10** | Gate PR. Its own trust-guard CI fails by design until #19 clears fish-com heater claims and #26 clears lizard. After #19 + #26 merge, re-run CI; should go green. |
| **30** | PR-D: sitemap freshness gate — `--check` mode + qc.yml job | `build-bot/sitemap-freshness-check` | BLOCKED-BY | requires a follow-up PR to clear fish-com sitemap drift before merge | **— (defer)** | Gate PR. Author explicitly says "do not merge" until fish-com sitemap drift PR exists. That follow-up PR does not exist yet. Leave for next week. |

## Recommended Saturday merge sequence

Run the merges in this order; do not parallelize — each merge changes the base
SHA and the downstream PRs need to rebase before their CI is meaningful.

1. **#19** — fish-com launch blockers (unlocks metadata-policy across the board)
2. **#26** — Lizard.com launch blockers
3. **#27** — Vets.co launch blockers
4. *(re-run CI on remaining open PRs — most should now go green)*
5. **#20** — Fish equipment recommender (new tool route)
6. **#21** — Vets.co visual polish
7. **#22** — Fish.com visual polish
8. **#23** — Saddle.com visual polish
9. **#24** — Lizard.com visual polish (rebase first to pick up #26)
10. **#28** — Dog.com visual + audit *(review carefully; flagship)*
11. **#29** — trust-guard baseline (CI will go green once #19 + #26 are in main)

That's a single morning of merges if each CI run is ~1 min. Realistic call:
plan for half a day with the rebase shuffles.

## Do-not-touch list (defer to next week)

- **#30** — sitemap freshness gate. Author explicitly blocks on a fish-com
  sitemap-drift follow-up PR that does not yet exist. Leave open; address with
  one of the in-flight branches (fish-com sitemap priority fix sounds like the
  natural companion).
- **DESIGN-AUDIT-VISUAL.md bundled inside #28** — flag to extract into
  `ops/handoffs/` rather than shipping in an app PR; surface this in PR review
  before merging #28.
- **Anything from the new overnight branches** (Horses+PetFood scaffold, etc.) —
  not in this triage; review separately when those PRs open.

## Definition of done

- All 9 ship-recommended PRs (#19, #26, #27, #20, #21, #22, #23, #24, #28) merged
  into main with green CI.
- #29 merged once its blockers (#19, #26) are in and re-run CI is green.
- #30 left open with a comment pointing at the required fish-com sitemap follow-up.
- Saturday ends with 1 open PR (#30) plus whatever the overnight agent branches
  produced.

## Ship-class summary

- **READY:** 5 (#20, #21, #22, #23, #24)
- **NEEDS FIX:** 4 (#19, #26, #27, #28) — all 4 fail only on the inherited
  metadata-policy gate; #19 IS the fix and unblocks the rest
- **BLOCKED-BY:** 2 (#29 → #19+#26; #30 → not-yet-existing follow-up)
- **SUPERSEDED:** 0
- **CLOSE:** 0
