---
from: COO
to: Carlo (CSRO) / IR
status: complete
created: 2026-06-09
next_action: Carlo to run sync-images.mjs + pixel-review the candidates; the single remaining gate is VISUAL differentiation.
---

# Launch-readiness scorecard — the 6 IR candidates

Synthesis of this session's work against the §8a launch-quality bar. **Headline: all 6 candidates are GREEN on code, trust, monetization, disclosure, schema, and tools. The single remaining gate to "premium/launch-ready" is VISUAL DIFFERENTIATION (your `sync-images.mjs` run + Visual Bot per-item queries) + the IR/Carlo pixel review.**

## Per-bar status (all candidates)
| Bar | State |
|---|---|
| Claim hygiene / trust | ✅ swept portfolio-wide + final residual sweep (PR #680); trust-guard hardened + self-enforcing |
| Result/intent monetization | ✅ tools → product/insurance paths; intent links; conservative claims (this wave) |
| `/go` routing + no leakage | ✅ all CTAs via existing `/go`; no page-level leaks |
| Disclosures (subtle, above surface) | ✅ incl. Lizard placement fix + PetFood compare cards |
| §1.5 clinical monetization | ✅ closed (Lizard acute removed / deficiency framed; no Rx/therapeutic monetized) |
| BreadcrumbList schema | ✅ single-source portfolio-wide (one per page) |
| Tools/calculators working | ✅ all real + now monetized result paths |
| Hub→spoke internal linking | ✅ |
| **Visual differentiation** | ❌ **BLOCKING — needs sync** (per-item photos; "no placeholders ≠ premium") |

## Per-candidate — closeness to launch-ready (visual dependency is the differentiator)
| # | Site | Trust/Monetization/Structure | Visual gap | Closeness |
|---|---|---|---|---|
| 1 | **Vets** | ✅ estimator conversion, funnel clean, insurance jewel | LOW — hubs synced; detail pages text-by-design | **Closest** — gap is pixel review only |
| 3 | **Ferret** | ✅ starter-kit paths, trust clean | LOW — already image-complete | **Close** |
| 5 | **Lizard** | ✅ clinical cleanup, tools, disclosures | LOW — keys synced | **Close** |
| 4 | **PetFood** | ✅ compare/tools monetized (no Rx), disclosures | MILD — brand pages render category-level | Near |
| 2 | **Dog** | ✅ intent paths, funnel clean (flagship) | HIGH — 44 breed tiles show the same category photo until sync | **Needs sync** |
| 6 | **Fish** | ✅ 6 tools → product kits | HIGH — species pages show category photo until sync | **Needs sync** |

## The one action that moves the needle
Run `sync-images.mjs` (your Mac) after Visual Bot confirms per-item queries. That populates the per-breed / per-species / per-brand photography (the queries are sound — the last run was rate-limited, not bad prompts). Vets/Ferret/Lizard barely depend on it; Dog/Fish depend on it most.

## Suggested launch sequencing (post-sync + pixel review)
1. **Vets** — least visual-dependent, highest revenue-per-visitor (insurance). True first candidate.
2. **Dog** — flagship traffic, but wait for the breed-photo sync to read premium.
3. Ferret / Lizard / PetFood / Fish as the sync + pixel review clear them.

No site is called premium here — that's IR/Carlo's pixel-review call.
